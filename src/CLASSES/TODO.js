export default class TODO{
    constructor(title,content,dueBy,prio,position){
        this.title = title;
        this.content = content;
        this.dueBy = dueBy;
        this.prio = prio;
        this.position = position;
    }
    // All methods below
};